window.addEventListener("load", function() {
    // Optionally trigger the animation with a delay if needed
    setTimeout(() => {
      document.querySelector('.animated-line').classList.add('visible');
    }, 500); // 500ms delay before line appears
  });
  